#!/bin/sh
# Stub: copy a minimal PDF placeholder to the output path
OUTPUT=""
while [ $# -gt 0 ]; do
  case "$1" in
    -o) OUTPUT="$2"; shift 2 ;;
    *) shift ;;
  esac
done
printf '%PDF-1.4 stub' > "$OUTPUT"
